package by.it.seledtsova.jd_09;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

class Parser {
    /*
    Var evaluate (String expression) {
        expression.replaceAll("\\s+","");
       String[] parts=expression.split(Patterns.OPERATION, 2);
        Var leftVar=VarCreator.build(parts[0]);
        if (parts.length<2) {
            return leftVar;
        }
        Var rightVar=VarCreator.build(parts[1]);
       Pattern matcherOperation= Pattern.compile(Pattern.OPERATION);
        Matcher matcherOperation=patternOperatin.matcher (expression);


        return null;
    }

     */
}
