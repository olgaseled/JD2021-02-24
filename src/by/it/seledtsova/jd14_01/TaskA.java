package by.it.seledtsova.jd14_01;

import java.io.File;

public class TaskA {
    /*
    public static void main(String[] args) {
       String fileName= "dataTaskA.bin";
        Class<TaskA> currentStructure = TaskA.class;


       String rootProject=System.getProperty("user.dir");
       String src=rootProject+ File.separator+"src"+File.separator;// свойство системы.
        Class<TaskA> currentStructure = TaskA.class;
        String relatedDir=currentStructure
                .getName()
                .replace(currentStructure.getSimpleName(), "")
                .replace(".",File.separator)
                String filePath=src+relatedDir+fileName;
            }

     */
}
